create table smart_home.responsaveis (
    id_responsaveis     INT             NOT NULL    PRIMARY KEY
,   nome                VARCHAR(20)     NOT NULL
,   sobrenome           VARCHAR(50)     NOT NULL
,   profissao           VARCHAR(50)
,   vinculo             varchar(20)     NOT NULL
);